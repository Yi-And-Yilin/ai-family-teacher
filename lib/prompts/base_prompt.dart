/// 基础系统提示词
/// 所有智能体共享的基础身份和能力定义
///
/// ⚠️ 注意：此文件的内容已迁移到 `study_buddy_prompt.dart`。
/// baseSystemPrompt 现在导出为 studyBuddySystemPrompt 的别名。
/// 如需修改系统提示词，请直接编辑 `study_buddy_prompt.dart`。

import 'study_buddy_prompt.dart';

/// 向后兼容别名
const String baseSystemPrompt = studyBuddySystemPrompt;
